name: Legacy Overleaf pull disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement
        run: |
          echo "This legacy workflow has been replaced by the bidirectional synchronization workflow."
